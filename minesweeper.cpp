#include "function.h"
int main()
{
    playGame();
    // vector<vector<char>> board = createBoard(38, 11, 23, 5);
    // printBoard(board);
    // vector<int> click({20,0});
    // board = updateBoard(board, click);
    // cout << endl;
    // cout << endl;
    // cout << endl;
    // printBoard(board);
    return 0;
}